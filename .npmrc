# Allow legacy peer dependencies for @anthropic-ai/claude-agent-sdk
# The SDK requires zod@^4.0.0 but we use zod@3.x
# This is safe because zod v3 and v4 have compatible APIs for our use case
legacy-peer-deps=true

